# Shared Postgres / Supabase (direct URI, port 5432 — not the 6543 pooler).
# StealthPay only creates and uses the `stealthpay` schema. Payrun uses `payrun`.
# Point both apps at the same database; tables never overlap.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"
